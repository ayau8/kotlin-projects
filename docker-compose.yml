services:
  mysql_db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: task_user_root_pass!
      MYSQL_DATABASE: task_manager_db
      MYSQL_USER: task_user
      MYSQL_PASSWORD: StrongP@ssw0rd123
    ports:
      - "3306:3306"

volumes:
  mysql_data:
    driver: local
